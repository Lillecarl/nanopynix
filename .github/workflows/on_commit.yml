# GENERATED FILE -- do not edit by hand.
# Edit ci/workflows/on_commit.nix; a pytest run renders this file again.

name: On commit
'on':
  push:
    branches-ignore:
    - ci-develop
    - gc-soak
  workflow_dispatch:
    inputs:
      jobs:
        type: string
        description: Comma-separated job IDs to run; leave empty for the full matrix
        required: false
        default: ''
jobs:
  static-checks:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Run the gates (ruff, ruff-strict, ruff format, pyright, shellcheck, grpclib-transports)
      timeout-minutes: 20
      run: |
        nix build --no-link --print-build-logs --keep-going \
          ".#check-lint" ".#check-lint-strict" ".#check-format" ".#check-types" \
          ".#check-shell" ".#check-grpclib-transports"
    timeout-minutes: 75
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',static-checks,')
    runs-on: ubuntu-24.04
  commit-subjects:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
      with:
        fetch-depth: 0
    - name: Check the Conventional Commits subject of each pushed commit
      timeout-minutes: 5
      run: |
        set -euo pipefail

        before="${{ github.event.before }}"
        after="${{ github.sha }}"

        # `before` is the all-zero SHA for a new branch, and it names a
        # commit that the remote no longer has after a force push. The
        # range means nothing in either case, so check the head alone.
        if git cat-file -e "$before^{commit}" 2>/dev/null; then
          commits=$(git rev-list --no-merges "$before..$after")
        else
          echo "no usable base commit; checking $after alone"
          commits=$(git rev-list --no-merges -1 "$after")
        fi

        # Say how many, so that a pass is auditable. A range with no
        # commits also exits 0, and a gate that quietly checks nothing
        # reads exactly like a gate that checked everything.
        echo "checking $(printf '%s\n' "$commits" | grep -c .) commit subject(s)"

        status=0
        for sha in $commits; do
          subject=$(git log -1 --format=%s "$sha")
          # A space is legal inside the parentheses, because this
          # repository writes a multi-scope subject as `(nanopynix, ekn)`.
          if ! printf '%s\n' "$subject" | grep -Eq '^[a-z]+(\([a-z0-9._/, -]+\))?!?: .+'; then
            echo "::error::$sha: not a Conventional Commits subject: $subject"
            status=1
          fi
        done

        if [ "$status" -ne 0 ]; then
          echo "CLAUDE.md requires the Conventional Commits prefix, for example 'feat(scope):' or 'fix(tests):'."
        fi
        exit "$status"
    timeout-minutes: 30
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',commit-subjects,')
    runs-on: ubuntu-24.04
  docs-build:
    needs:
    - commit-subjects
    - static-checks
    - test-asan-git
    - test-asan-nix_2_31
    - test-asan-nix_2_34
    - test-asan-nix_2_35
    - test-daemon-git
    - test-daemon-nix_2_31
    - test-daemon-nix_2_34
    - test-daemon-nix_2_35
    - test-local-git
    - test-local-nix_2_31
    - test-local-nix_2_34
    - test-local-nix_2_35
    - test-nogc-git
    - test-nogc-nix_2_31
    - test-nogc-nix_2_34
    - test-nogc-nix_2_35
    - test-tsan-git
    - test-tsan-nix_2_34
    - test-tsan-nix_2_35
    - test-ubsan-git
    - test-ubsan-nix_2_31
    - test-ubsan-nix_2_34
    - test-ubsan-nix_2_35
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build documentation
      timeout-minutes: 30
      run: nix build .#nanopynix-docs --out-link result --print-build-logs --print-out-paths
    - name: Verify docs closure
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Prepare Pages artifact
      timeout-minutes: 5
      run: |
        mkdir -p public
        cp -r --no-preserve=mode,ownership result/. public/
    - timeout-minutes: 15
      uses: actions/upload-pages-artifact@main
      with:
        path: public
    timeout-minutes: 120
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-24.04
  docs-deploy:
    needs: docs-build
    steps:
    - name: Deploy to GitHub Pages
      timeout-minutes: 20
      uses: actions/deploy-pages@main
      id: deployment
    timeout-minutes: 35
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    concurrency:
      group: pages
      cancel-in-progress: false
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-24.04
  test-asan-git:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build ASAN nanopynix test runner (git)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-git-asan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run ASAN-instrumented suite (git, local backend)
      timeout-minutes: 60
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/asan-output-git.log"
        status=0
        # **`tests/pynix` is out, and run 30895974566 is the reason.**
        # That run reached the 120-minute cap, and its log says what it
        # bought:
        #
        #   806 tests executed, 742 passed, 10 failed, 54 skipped
        #   0 AddressSanitizer reports
        #   killed inside tests/pynix/test_lsp.py
        #
        # **All ten failures were in `tests/pynix`, and none were in
        # `tests/nanopynix`.** Two hours of ASAN found no memory error
        # anywhere, so nothing is lost by the cut and the whole failure
        # set goes with it.
        #
        # The reason it can go is that only instrumented code reports:
        # nanopynix-bindings, the Nix libraries and boost.
        # `tests/nanopynix` drives that surface directly.
        # `tests/pynix` tests a CLI and an LSP server through
        # uninstrumented Python, and reaches the bindings only along
        # paths `tests/nanopynix` already covers. It is also where #44
        # lives.
        #
        # The in-process engine stays, so the acceptance test of issue
        # #35 keeps its ASAN cover: a test that builds an evaluator in
        # the pytest process runs in a fork of it.
        #
        # **Three deadlines, and the default of each one is written for
        # a build with no instrumentation.** Run 30891726124 measured
        # what happens when they stay: tests failed on a clock rather
        # than on an assertion about Nix.
        #
        #   NANOPYNIX_RPC_TIMEOUT      default 300. Six tests died with
        #                              Status.DEADLINE_EXCEEDED at 120.
        #   NANOPYNIX_SHUTDOWN_TIMEOUT default 5. The worker cannot
        #                              answer `Shutdown` in five
        #                              seconds under ASAN.
        #   NANOPYNIX_TEST_TIMEOUT     default 120 (`tests/conftest.py`,
        #                              whose own comment asks for this
        #                              override "for slower
        #                              environments").
        #
        # The first answer to that was 600/120/900, and it cost more
        # than it saved: throughput fell from 15.0 tests a minute to
        # 6.7, because each of the ten failures then waited out a much
        # longer deadline. A deadline exists to make a hang visible,
        # not to sit through one. These are the middle ground, and this
        # job is the measurement run for tighter ones. See #61.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          --ignore=tests/pynix \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so read the log rather
        # than trusting the exit status alone: `halt_on_error=1` kills
        # the process that reports, but a report raised inside a
        # forkserver worker can still be reaped into a plain test
        # failure. mkUbsanTestJob says the same thing.
        if grep -qE "AddressSanitizer|LeakSanitizer" "$LOGFILE"; then
          echo "::error::sanitizer report on git -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (ASAN, git)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload ASAN output (git)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: asan-report-git
        path: ${{ github.workspace }}/asan-output-git.log
      if: ${{ !cancelled() }}
    timeout-minutes: 210
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-asan-git,')
    runs-on: ubuntu-24.04
  test-asan-nix_2_31:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build ASAN nanopynix test runner (nix_2_31)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-nix_2_31-asan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run ASAN-instrumented suite (nix_2_31, local backend)
      timeout-minutes: 60
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/asan-output-nix_2_31.log"
        status=0
        # **`tests/pynix` is out, and run 30895974566 is the reason.**
        # That run reached the 120-minute cap, and its log says what it
        # bought:
        #
        #   806 tests executed, 742 passed, 10 failed, 54 skipped
        #   0 AddressSanitizer reports
        #   killed inside tests/pynix/test_lsp.py
        #
        # **All ten failures were in `tests/pynix`, and none were in
        # `tests/nanopynix`.** Two hours of ASAN found no memory error
        # anywhere, so nothing is lost by the cut and the whole failure
        # set goes with it.
        #
        # The reason it can go is that only instrumented code reports:
        # nanopynix-bindings, the Nix libraries and boost.
        # `tests/nanopynix` drives that surface directly.
        # `tests/pynix` tests a CLI and an LSP server through
        # uninstrumented Python, and reaches the bindings only along
        # paths `tests/nanopynix` already covers. It is also where #44
        # lives.
        #
        # The in-process engine stays, so the acceptance test of issue
        # #35 keeps its ASAN cover: a test that builds an evaluator in
        # the pytest process runs in a fork of it.
        #
        # **Three deadlines, and the default of each one is written for
        # a build with no instrumentation.** Run 30891726124 measured
        # what happens when they stay: tests failed on a clock rather
        # than on an assertion about Nix.
        #
        #   NANOPYNIX_RPC_TIMEOUT      default 300. Six tests died with
        #                              Status.DEADLINE_EXCEEDED at 120.
        #   NANOPYNIX_SHUTDOWN_TIMEOUT default 5. The worker cannot
        #                              answer `Shutdown` in five
        #                              seconds under ASAN.
        #   NANOPYNIX_TEST_TIMEOUT     default 120 (`tests/conftest.py`,
        #                              whose own comment asks for this
        #                              override "for slower
        #                              environments").
        #
        # The first answer to that was 600/120/900, and it cost more
        # than it saved: throughput fell from 15.0 tests a minute to
        # 6.7, because each of the ten failures then waited out a much
        # longer deadline. A deadline exists to make a hang visible,
        # not to sit through one. These are the middle ground, and this
        # job is the measurement run for tighter ones. See #61.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          --ignore=tests/pynix \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so read the log rather
        # than trusting the exit status alone: `halt_on_error=1` kills
        # the process that reports, but a report raised inside a
        # forkserver worker can still be reaped into a plain test
        # failure. mkUbsanTestJob says the same thing.
        if grep -qE "AddressSanitizer|LeakSanitizer" "$LOGFILE"; then
          echo "::error::sanitizer report on nix_2_31 -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (ASAN, nix_2_31)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload ASAN output (nix_2_31)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: asan-report-nix_2_31
        path: ${{ github.workspace }}/asan-output-nix_2_31.log
      if: ${{ !cancelled() }}
    timeout-minutes: 210
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-asan-nix_2_31,')
    runs-on: ubuntu-24.04
  test-asan-nix_2_34:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build ASAN nanopynix test runner (nix_2_34)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-nix_2_34-asan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run ASAN-instrumented suite (nix_2_34, local backend)
      timeout-minutes: 60
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/asan-output-nix_2_34.log"
        status=0
        # **`tests/pynix` is out, and run 30895974566 is the reason.**
        # That run reached the 120-minute cap, and its log says what it
        # bought:
        #
        #   806 tests executed, 742 passed, 10 failed, 54 skipped
        #   0 AddressSanitizer reports
        #   killed inside tests/pynix/test_lsp.py
        #
        # **All ten failures were in `tests/pynix`, and none were in
        # `tests/nanopynix`.** Two hours of ASAN found no memory error
        # anywhere, so nothing is lost by the cut and the whole failure
        # set goes with it.
        #
        # The reason it can go is that only instrumented code reports:
        # nanopynix-bindings, the Nix libraries and boost.
        # `tests/nanopynix` drives that surface directly.
        # `tests/pynix` tests a CLI and an LSP server through
        # uninstrumented Python, and reaches the bindings only along
        # paths `tests/nanopynix` already covers. It is also where #44
        # lives.
        #
        # The in-process engine stays, so the acceptance test of issue
        # #35 keeps its ASAN cover: a test that builds an evaluator in
        # the pytest process runs in a fork of it.
        #
        # **Three deadlines, and the default of each one is written for
        # a build with no instrumentation.** Run 30891726124 measured
        # what happens when they stay: tests failed on a clock rather
        # than on an assertion about Nix.
        #
        #   NANOPYNIX_RPC_TIMEOUT      default 300. Six tests died with
        #                              Status.DEADLINE_EXCEEDED at 120.
        #   NANOPYNIX_SHUTDOWN_TIMEOUT default 5. The worker cannot
        #                              answer `Shutdown` in five
        #                              seconds under ASAN.
        #   NANOPYNIX_TEST_TIMEOUT     default 120 (`tests/conftest.py`,
        #                              whose own comment asks for this
        #                              override "for slower
        #                              environments").
        #
        # The first answer to that was 600/120/900, and it cost more
        # than it saved: throughput fell from 15.0 tests a minute to
        # 6.7, because each of the ten failures then waited out a much
        # longer deadline. A deadline exists to make a hang visible,
        # not to sit through one. These are the middle ground, and this
        # job is the measurement run for tighter ones. See #61.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          --ignore=tests/pynix \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so read the log rather
        # than trusting the exit status alone: `halt_on_error=1` kills
        # the process that reports, but a report raised inside a
        # forkserver worker can still be reaped into a plain test
        # failure. mkUbsanTestJob says the same thing.
        if grep -qE "AddressSanitizer|LeakSanitizer" "$LOGFILE"; then
          echo "::error::sanitizer report on nix_2_34 -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (ASAN, nix_2_34)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload ASAN output (nix_2_34)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: asan-report-nix_2_34
        path: ${{ github.workspace }}/asan-output-nix_2_34.log
      if: ${{ !cancelled() }}
    timeout-minutes: 210
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-asan-nix_2_34,')
    runs-on: ubuntu-24.04
  test-asan-nix_2_35:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build ASAN nanopynix test runner (nix_2_35)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-nix_2_35-asan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run ASAN-instrumented suite (nix_2_35, local backend)
      timeout-minutes: 60
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/asan-output-nix_2_35.log"
        status=0
        # **`tests/pynix` is out, and run 30895974566 is the reason.**
        # That run reached the 120-minute cap, and its log says what it
        # bought:
        #
        #   806 tests executed, 742 passed, 10 failed, 54 skipped
        #   0 AddressSanitizer reports
        #   killed inside tests/pynix/test_lsp.py
        #
        # **All ten failures were in `tests/pynix`, and none were in
        # `tests/nanopynix`.** Two hours of ASAN found no memory error
        # anywhere, so nothing is lost by the cut and the whole failure
        # set goes with it.
        #
        # The reason it can go is that only instrumented code reports:
        # nanopynix-bindings, the Nix libraries and boost.
        # `tests/nanopynix` drives that surface directly.
        # `tests/pynix` tests a CLI and an LSP server through
        # uninstrumented Python, and reaches the bindings only along
        # paths `tests/nanopynix` already covers. It is also where #44
        # lives.
        #
        # The in-process engine stays, so the acceptance test of issue
        # #35 keeps its ASAN cover: a test that builds an evaluator in
        # the pytest process runs in a fork of it.
        #
        # **Three deadlines, and the default of each one is written for
        # a build with no instrumentation.** Run 30891726124 measured
        # what happens when they stay: tests failed on a clock rather
        # than on an assertion about Nix.
        #
        #   NANOPYNIX_RPC_TIMEOUT      default 300. Six tests died with
        #                              Status.DEADLINE_EXCEEDED at 120.
        #   NANOPYNIX_SHUTDOWN_TIMEOUT default 5. The worker cannot
        #                              answer `Shutdown` in five
        #                              seconds under ASAN.
        #   NANOPYNIX_TEST_TIMEOUT     default 120 (`tests/conftest.py`,
        #                              whose own comment asks for this
        #                              override "for slower
        #                              environments").
        #
        # The first answer to that was 600/120/900, and it cost more
        # than it saved: throughput fell from 15.0 tests a minute to
        # 6.7, because each of the ten failures then waited out a much
        # longer deadline. A deadline exists to make a hang visible,
        # not to sit through one. These are the middle ground, and this
        # job is the measurement run for tighter ones. See #61.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          --ignore=tests/pynix \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so read the log rather
        # than trusting the exit status alone: `halt_on_error=1` kills
        # the process that reports, but a report raised inside a
        # forkserver worker can still be reaped into a plain test
        # failure. mkUbsanTestJob says the same thing.
        if grep -qE "AddressSanitizer|LeakSanitizer" "$LOGFILE"; then
          echo "::error::sanitizer report on nix_2_35 -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (ASAN, nix_2_35)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=180 NANOPYNIX_SHUTDOWN_TIMEOUT=60 NANOPYNIX_TEST_TIMEOUT=300 NANOPYNIX_TEST_SANITIZER=asan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload ASAN output (nix_2_35)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: asan-report-nix_2_35
        path: ${{ github.workspace }}/asan-output-nix_2_35.log
      if: ${{ !cancelled() }}
    timeout-minutes: 210
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-asan-nix_2_35,')
    runs-on: ubuntu-24.04
  test-nogc-git:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build no-collector nanopynix test runner (git)
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-git-nogc" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run the suite without a collector (git, local backend)
      timeout-minutes: 30
      run: |
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak"
    - name: Run the concurrency soak (no collector, git)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    timeout-minutes: 135
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-nogc-git,')
    runs-on: ubuntu-24.04
  test-nogc-nix_2_31:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build no-collector nanopynix test runner (nix_2_31)
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_31-nogc" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run the suite without a collector (nix_2_31, local backend)
      timeout-minutes: 30
      run: |
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak"
    - name: Run the concurrency soak (no collector, nix_2_31)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    timeout-minutes: 135
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-nogc-nix_2_31,')
    runs-on: ubuntu-24.04
  test-nogc-nix_2_34:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build no-collector nanopynix test runner (nix_2_34)
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_34-nogc" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run the suite without a collector (nix_2_34, local backend)
      timeout-minutes: 30
      run: |
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak"
    - name: Run the concurrency soak (no collector, nix_2_34)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    timeout-minutes: 135
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-nogc-nix_2_34,')
    runs-on: ubuntu-24.04
  test-nogc-nix_2_35:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build no-collector nanopynix test runner (nix_2_35)
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_35-nogc" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run the suite without a collector (nix_2_35, local backend)
      timeout-minutes: 30
      run: |
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak"
    - name: Run the concurrency soak (no collector, nix_2_35)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    timeout-minutes: 135
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-nogc-nix_2_35,')
    runs-on: ubuntu-24.04
  test-ubsan-git:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build UBSAN nanopynix test runner (git)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-git-ubsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run UBSAN-instrumented suite (git, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/ubsan-output-git.log"
        status=0
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so grep for it rather than
        # trusting the exit status alone: halt_on_error=1 makes UBSan
        # kill the process, but a report raised inside a forkserver
        # worker can still be reaped into a plain test failure.
        #
        # "Unexpected condition" is the message of `nix::unreachable`,
        # which is what `nixUnreachableWhenHardened` becomes once
        # `NIX_UBSAN_ENABLED` is on. That path never prints the word
        # "runtime error", so the first two patterns would miss it.
        if grep -qE "(UndefinedBehaviorSanitizer|runtime error):|Unexpected condition in " "$LOGFILE"; then
          echo "::error::sanitizer report on git -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (UBSAN, git)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload UBSAN output (git)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: ubsan-report-git
        path: ${{ github.workspace }}/ubsan-output-git.log
      if: ${{ !cancelled() }}
    timeout-minutes: 180
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-ubsan-git,')
    runs-on: ubuntu-24.04
  test-ubsan-nix_2_31:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build UBSAN nanopynix test runner (nix_2_31)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-nix_2_31-ubsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run UBSAN-instrumented suite (nix_2_31, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/ubsan-output-nix_2_31.log"
        status=0
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so grep for it rather than
        # trusting the exit status alone: halt_on_error=1 makes UBSan
        # kill the process, but a report raised inside a forkserver
        # worker can still be reaped into a plain test failure.
        #
        # "Unexpected condition" is the message of `nix::unreachable`,
        # which is what `nixUnreachableWhenHardened` becomes once
        # `NIX_UBSAN_ENABLED` is on. That path never prints the word
        # "runtime error", so the first two patterns would miss it.
        if grep -qE "(UndefinedBehaviorSanitizer|runtime error):|Unexpected condition in " "$LOGFILE"; then
          echo "::error::sanitizer report on nix_2_31 -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (UBSAN, nix_2_31)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload UBSAN output (nix_2_31)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: ubsan-report-nix_2_31
        path: ${{ github.workspace }}/ubsan-output-nix_2_31.log
      if: ${{ !cancelled() }}
    timeout-minutes: 180
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-ubsan-nix_2_31,')
    runs-on: ubuntu-24.04
  test-ubsan-nix_2_34:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build UBSAN nanopynix test runner (nix_2_34)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-nix_2_34-ubsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run UBSAN-instrumented suite (nix_2_34, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/ubsan-output-nix_2_34.log"
        status=0
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so grep for it rather than
        # trusting the exit status alone: halt_on_error=1 makes UBSan
        # kill the process, but a report raised inside a forkserver
        # worker can still be reaped into a plain test failure.
        #
        # "Unexpected condition" is the message of `nix::unreachable`,
        # which is what `nixUnreachableWhenHardened` becomes once
        # `NIX_UBSAN_ENABLED` is on. That path never prints the word
        # "runtime error", so the first two patterns would miss it.
        if grep -qE "(UndefinedBehaviorSanitizer|runtime error):|Unexpected condition in " "$LOGFILE"; then
          echo "::error::sanitizer report on nix_2_34 -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (UBSAN, nix_2_34)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload UBSAN output (nix_2_34)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: ubsan-report-nix_2_34
        path: ${{ github.workspace }}/ubsan-output-nix_2_34.log
      if: ${{ !cancelled() }}
    timeout-minutes: 180
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-ubsan-nix_2_34,')
    runs-on: ubuntu-24.04
  test-ubsan-nix_2_35:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build UBSAN nanopynix test runner (nix_2_35)
      timeout-minutes: 60
      run: nix build ".#nanopynix-tests-nix_2_35-ubsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run UBSAN-instrumented suite (nix_2_35, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/ubsan-output-nix_2_35.log"
        status=0
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          2>&1 | tee -a "$LOGFILE" || status=$?
        # A sanitizer report is the finding, so grep for it rather than
        # trusting the exit status alone: halt_on_error=1 makes UBSan
        # kill the process, but a report raised inside a forkserver
        # worker can still be reaped into a plain test failure.
        #
        # "Unexpected condition" is the message of `nix::unreachable`,
        # which is what `nixUnreachableWhenHardened` becomes once
        # `NIX_UBSAN_ENABLED` is on. That path never prints the word
        # "runtime error", so the first two patterns would miss it.
        if grep -qE "(UndefinedBehaviorSanitizer|runtime error):|Unexpected condition in " "$LOGFILE"; then
          echo "::error::sanitizer report on nix_2_35 -- see the log above"
          exit 1
        fi
        exit "$status"
    - name: Run the concurrency soak (UBSAN, nix_2_35)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=60 NANOPYNIX_TEST_SANITIZER=ubsan \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload UBSAN output (nix_2_35)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: ubsan-report-nix_2_35
        path: ${{ github.workspace }}/ubsan-output-nix_2_35.log
      if: ${{ !cancelled() }}
    timeout-minutes: 180
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-ubsan-nix_2_35,')
    runs-on: ubuntu-24.04
  test-tsan-git:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build TSAN nanopynix test runner (git)
      timeout-minutes: 45
      run: nix build ".#nanopynix-tests-git-tsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run TSAN-instrumented concurrency soak (five seeds, local+daemon backends)
      timeout-minutes: 25
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/tsan-output-git.log"
        # Boehm suspends every other thread with a signal before it
        # collects, and TSAN intercepts signal delivery. When Boehm
        # cannot stop the world it calls `abort`, and the process dies
        # with status 134 and reports no race. See issue #69, which
        # carries the measurement: the rate is near one run in four,
        # and it does not depend on anything this repository sets.
        #
        # **Boehm's own answer to this is already in the build.**
        # `gc_priv.h` suspends a thread with the synchronous `SIGSYS`
        # when `THREAD_SANITIZER` is defined, and `gcconfig.h` defines
        # it from `__has_feature(thread_sanitizer)`. GCC 15.3 supports
        # `__has_feature` and reports the feature, so the collector
        # this job links already uses `SIGSYS`, and it aborts anyway.
        # Issue #69 carries the proof. So there is no known correction
        # yet, and the two lines below are what this job does instead.
        #
        # `GC_INITIAL_HEAP_SIZE` gives Boehm a heap it does not have
        # to grow, and a collection that never happens cannot fail to
        # stop the world. Nix sets this itself when the variable is
        # absent, to 25% of RAM capped at 384 MiB
        # (libexpr/eval-gc.cc), and the soak exhausts that: it holds
        # 66 tests and their evaluators in one process. The cap is
        # what this raises, and `GC_expand_hp` takes virtual rather
        # than resident memory, so the runner pays little for it.
        # **This lowers the rate, and corrects nothing.** A full
        # matrix run aborted with it in place.
        #
        # The retry is what remains for the aborts that still happen.
        # An abort says nothing about the code, so retry the seed. A
        # budget for the whole job, and not N attempts for each seed,
        # is what keeps the worst case inside `tsanStress`.
        #
        # This tolerates only that one signature. A race still fails
        # the job, and so does any other non-zero status.
        ABORT_BUDGET=5
        aborts=0
        race_found=0
        real_failure=0
        for i in $(seq 1 5); do
          attempt=0
          while : ; do
            attempt=$((attempt + 1))
            echo "=== TSAN run $i attempt $attempt ===" | tee -a "$LOGFILE"
            RUNLOG="$(mktemp)"
            # Boehm's own log, which the abort branch below reads.
            # `GC_PRINT_STATS` turns on `GC_COND_LOG_PRINTF`, and
            # `resend_lost_signals` uses it to report how many threads
            # still owe an acknowledgement on each pass. That count is
            # the first thing issue #69 needs. `GC_LOG_FILE` keeps the
            # rest of the collector chatter out of the job log.
            GCLOG="$(mktemp)"
            status=0
            unshare --user --map-root-user --mount --pid --fork --mount-proc env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=30 NANOPYNIX_TEST_SANITIZER=tsan PYTHONDONTWRITEBYTECODE=1 GC_INITIAL_HEAP_SIZE=2147483648 GC_PRINT_STATS=1 GC_LOG_FILE="$GCLOG" \
              ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local,daemon \
              -m soak --soak-seed="$i" \
              --soak-report="${{ github.workspace }}/soak-git-run$i.json" \
              2>&1 | tee "$RUNLOG" || status=$?
            cat "$RUNLOG" >> "$LOGFILE"
            echo "=== TSAN run $i attempt $attempt exit status: $status ===" | tee -a "$LOGFILE"
            # Read the attempt, and not the whole log. The log holds
            # every earlier attempt, so a match there says nothing
            # about this one.
            if grep -q "ThreadSanitizer: data race" "$RUNLOG"; then
              echo "TSAN data race detected on run $i -- stopping early" | tee -a "$LOGFILE"
              race_found=1
              rm -f "$RUNLOG" "$GCLOG"
              break 2
            fi
            if [ "$status" -eq 134 ] && grep -q "Signals delivery fails" "$RUNLOG"; then
              aborts=$((aborts + 1))
              rm -f "$RUNLOG"
              echo "=== Boehm resend log of run $i attempt $attempt ===" | tee -a "$LOGFILE"
              grep -E "Resent|Lost some threads|stop_world" "$GCLOG" | tail -40 | tee -a "$LOGFILE" || true
              rm -f "$GCLOG"
              echo "Boehm could not stop the world on run $i (issue #69). Tolerated aborts: $aborts of $ABORT_BUDGET." | tee -a "$LOGFILE"
              if [ "$aborts" -gt "$ABORT_BUDGET" ]; then
                echo "::error::the collector aborted more than $ABORT_BUDGET times -- see issue #69"
                real_failure=1
                break 2
              fi
              continue
            fi
            rm -f "$RUNLOG" "$GCLOG"
            if [ "$status" -ne 0 ]; then
              echo "::error::run $i failed with status $status -- see log above"
              real_failure=1
              break 2
            fi
            break
          done
        done
        echo "=== TSAN soak summary: $aborts tolerated collector abort(s) of a budget of $ABORT_BUDGET ===" | tee -a "$LOGFILE"
        if [ "$race_found" -eq 1 ]; then
          echo "::error::genuine ThreadSanitizer data race detected -- see log above"
          exit 1
        fi
        if [ "$real_failure" -eq 1 ]; then
          exit 1
        fi
        exit 0
    - name: Run TSAN-instrumented concurrency tests (single pass, local+daemon backends)
      timeout-minutes: 20
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/tsan-output-broad-git.log"
        status=0
        unshare --user --map-root-user --mount --pid --fork --mount-proc env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=30 NANOPYNIX_TEST_SANITIZER=tsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local,daemon -m concurrency \
          2>&1 | tee -a "$LOGFILE" || status=$?
        echo "=== TSAN broad pass exit status: $status ===" | tee -a "$LOGFILE"
        if grep -q "ThreadSanitizer: data race" "$LOGFILE"; then
          echo "::error::genuine ThreadSanitizer data race detected -- see log above"
          exit 1
        fi
        if grep -qE "[0-9]+ (failed|error)|Fatal Python error|pthread_kill failed at suspend" "$LOGFILE"; then
          echo "::error::pytest reported a real failure, or the process crashed -- see log above"
          exit 1
        fi
        exit 0
    - name: Upload TSAN output (git)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: tsan-race-report-git
        path: |
          ${{ github.workspace }}/tsan-output-git.log
          ${{ github.workspace }}/tsan-output-broad-git.log
          ${{ github.workspace }}/soak-git-run*.json
      if: ${{ !cancelled() }}
    timeout-minutes: 165
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-tsan-git,')
    runs-on: ubuntu-24.04
  test-tsan-nix_2_34:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build TSAN nanopynix test runner (nix_2_34)
      timeout-minutes: 45
      run: nix build ".#nanopynix-tests-nix_2_34-tsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run TSAN-instrumented concurrency soak (five seeds, local+daemon backends)
      timeout-minutes: 25
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/tsan-output-nix_2_34.log"
        # Boehm suspends every other thread with a signal before it
        # collects, and TSAN intercepts signal delivery. When Boehm
        # cannot stop the world it calls `abort`, and the process dies
        # with status 134 and reports no race. See issue #69, which
        # carries the measurement: the rate is near one run in four,
        # and it does not depend on anything this repository sets.
        #
        # **Boehm's own answer to this is already in the build.**
        # `gc_priv.h` suspends a thread with the synchronous `SIGSYS`
        # when `THREAD_SANITIZER` is defined, and `gcconfig.h` defines
        # it from `__has_feature(thread_sanitizer)`. GCC 15.3 supports
        # `__has_feature` and reports the feature, so the collector
        # this job links already uses `SIGSYS`, and it aborts anyway.
        # Issue #69 carries the proof. So there is no known correction
        # yet, and the two lines below are what this job does instead.
        #
        # `GC_INITIAL_HEAP_SIZE` gives Boehm a heap it does not have
        # to grow, and a collection that never happens cannot fail to
        # stop the world. Nix sets this itself when the variable is
        # absent, to 25% of RAM capped at 384 MiB
        # (libexpr/eval-gc.cc), and the soak exhausts that: it holds
        # 66 tests and their evaluators in one process. The cap is
        # what this raises, and `GC_expand_hp` takes virtual rather
        # than resident memory, so the runner pays little for it.
        # **This lowers the rate, and corrects nothing.** A full
        # matrix run aborted with it in place.
        #
        # The retry is what remains for the aborts that still happen.
        # An abort says nothing about the code, so retry the seed. A
        # budget for the whole job, and not N attempts for each seed,
        # is what keeps the worst case inside `tsanStress`.
        #
        # This tolerates only that one signature. A race still fails
        # the job, and so does any other non-zero status.
        ABORT_BUDGET=5
        aborts=0
        race_found=0
        real_failure=0
        for i in $(seq 1 5); do
          attempt=0
          while : ; do
            attempt=$((attempt + 1))
            echo "=== TSAN run $i attempt $attempt ===" | tee -a "$LOGFILE"
            RUNLOG="$(mktemp)"
            # Boehm's own log, which the abort branch below reads.
            # `GC_PRINT_STATS` turns on `GC_COND_LOG_PRINTF`, and
            # `resend_lost_signals` uses it to report how many threads
            # still owe an acknowledgement on each pass. That count is
            # the first thing issue #69 needs. `GC_LOG_FILE` keeps the
            # rest of the collector chatter out of the job log.
            GCLOG="$(mktemp)"
            status=0
            unshare --user --map-root-user --mount --pid --fork --mount-proc env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=30 NANOPYNIX_TEST_SANITIZER=tsan PYTHONDONTWRITEBYTECODE=1 GC_INITIAL_HEAP_SIZE=2147483648 GC_PRINT_STATS=1 GC_LOG_FILE="$GCLOG" \
              ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local,daemon \
              -m soak --soak-seed="$i" \
              --soak-report="${{ github.workspace }}/soak-nix_2_34-run$i.json" \
              2>&1 | tee "$RUNLOG" || status=$?
            cat "$RUNLOG" >> "$LOGFILE"
            echo "=== TSAN run $i attempt $attempt exit status: $status ===" | tee -a "$LOGFILE"
            # Read the attempt, and not the whole log. The log holds
            # every earlier attempt, so a match there says nothing
            # about this one.
            if grep -q "ThreadSanitizer: data race" "$RUNLOG"; then
              echo "TSAN data race detected on run $i -- stopping early" | tee -a "$LOGFILE"
              race_found=1
              rm -f "$RUNLOG" "$GCLOG"
              break 2
            fi
            if [ "$status" -eq 134 ] && grep -q "Signals delivery fails" "$RUNLOG"; then
              aborts=$((aborts + 1))
              rm -f "$RUNLOG"
              echo "=== Boehm resend log of run $i attempt $attempt ===" | tee -a "$LOGFILE"
              grep -E "Resent|Lost some threads|stop_world" "$GCLOG" | tail -40 | tee -a "$LOGFILE" || true
              rm -f "$GCLOG"
              echo "Boehm could not stop the world on run $i (issue #69). Tolerated aborts: $aborts of $ABORT_BUDGET." | tee -a "$LOGFILE"
              if [ "$aborts" -gt "$ABORT_BUDGET" ]; then
                echo "::error::the collector aborted more than $ABORT_BUDGET times -- see issue #69"
                real_failure=1
                break 2
              fi
              continue
            fi
            rm -f "$RUNLOG" "$GCLOG"
            if [ "$status" -ne 0 ]; then
              echo "::error::run $i failed with status $status -- see log above"
              real_failure=1
              break 2
            fi
            break
          done
        done
        echo "=== TSAN soak summary: $aborts tolerated collector abort(s) of a budget of $ABORT_BUDGET ===" | tee -a "$LOGFILE"
        if [ "$race_found" -eq 1 ]; then
          echo "::error::genuine ThreadSanitizer data race detected -- see log above"
          exit 1
        fi
        if [ "$real_failure" -eq 1 ]; then
          exit 1
        fi
        exit 0
    - name: Run TSAN-instrumented concurrency tests (single pass, local+daemon backends)
      timeout-minutes: 20
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/tsan-output-broad-nix_2_34.log"
        status=0
        unshare --user --map-root-user --mount --pid --fork --mount-proc env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=30 NANOPYNIX_TEST_SANITIZER=tsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local,daemon -m concurrency \
          2>&1 | tee -a "$LOGFILE" || status=$?
        echo "=== TSAN broad pass exit status: $status ===" | tee -a "$LOGFILE"
        if grep -q "ThreadSanitizer: data race" "$LOGFILE"; then
          echo "::error::genuine ThreadSanitizer data race detected -- see log above"
          exit 1
        fi
        if grep -qE "[0-9]+ (failed|error)|Fatal Python error|pthread_kill failed at suspend" "$LOGFILE"; then
          echo "::error::pytest reported a real failure, or the process crashed -- see log above"
          exit 1
        fi
        exit 0
    - name: Upload TSAN output (nix_2_34)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: tsan-race-report-nix_2_34
        path: |
          ${{ github.workspace }}/tsan-output-nix_2_34.log
          ${{ github.workspace }}/tsan-output-broad-nix_2_34.log
          ${{ github.workspace }}/soak-nix_2_34-run*.json
      if: ${{ !cancelled() }}
    timeout-minutes: 165
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-tsan-nix_2_34,')
    runs-on: ubuntu-24.04
  test-tsan-nix_2_35:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build TSAN nanopynix test runner (nix_2_35)
      timeout-minutes: 45
      run: nix build ".#nanopynix-tests-nix_2_35-tsan" --out-link result --print-build-logs
        --print-out-paths
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Run TSAN-instrumented concurrency soak (five seeds, local+daemon backends)
      timeout-minutes: 25
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/tsan-output-nix_2_35.log"
        # Boehm suspends every other thread with a signal before it
        # collects, and TSAN intercepts signal delivery. When Boehm
        # cannot stop the world it calls `abort`, and the process dies
        # with status 134 and reports no race. See issue #69, which
        # carries the measurement: the rate is near one run in four,
        # and it does not depend on anything this repository sets.
        #
        # **Boehm's own answer to this is already in the build.**
        # `gc_priv.h` suspends a thread with the synchronous `SIGSYS`
        # when `THREAD_SANITIZER` is defined, and `gcconfig.h` defines
        # it from `__has_feature(thread_sanitizer)`. GCC 15.3 supports
        # `__has_feature` and reports the feature, so the collector
        # this job links already uses `SIGSYS`, and it aborts anyway.
        # Issue #69 carries the proof. So there is no known correction
        # yet, and the two lines below are what this job does instead.
        #
        # `GC_INITIAL_HEAP_SIZE` gives Boehm a heap it does not have
        # to grow, and a collection that never happens cannot fail to
        # stop the world. Nix sets this itself when the variable is
        # absent, to 25% of RAM capped at 384 MiB
        # (libexpr/eval-gc.cc), and the soak exhausts that: it holds
        # 66 tests and their evaluators in one process. The cap is
        # what this raises, and `GC_expand_hp` takes virtual rather
        # than resident memory, so the runner pays little for it.
        # **This lowers the rate, and corrects nothing.** A full
        # matrix run aborted with it in place.
        #
        # The retry is what remains for the aborts that still happen.
        # An abort says nothing about the code, so retry the seed. A
        # budget for the whole job, and not N attempts for each seed,
        # is what keeps the worst case inside `tsanStress`.
        #
        # This tolerates only that one signature. A race still fails
        # the job, and so does any other non-zero status.
        ABORT_BUDGET=5
        aborts=0
        race_found=0
        real_failure=0
        for i in $(seq 1 5); do
          attempt=0
          while : ; do
            attempt=$((attempt + 1))
            echo "=== TSAN run $i attempt $attempt ===" | tee -a "$LOGFILE"
            RUNLOG="$(mktemp)"
            # Boehm's own log, which the abort branch below reads.
            # `GC_PRINT_STATS` turns on `GC_COND_LOG_PRINTF`, and
            # `resend_lost_signals` uses it to report how many threads
            # still owe an acknowledgement on each pass. That count is
            # the first thing issue #69 needs. `GC_LOG_FILE` keeps the
            # rest of the collector chatter out of the job log.
            GCLOG="$(mktemp)"
            status=0
            unshare --user --map-root-user --mount --pid --fork --mount-proc env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=30 NANOPYNIX_TEST_SANITIZER=tsan PYTHONDONTWRITEBYTECODE=1 GC_INITIAL_HEAP_SIZE=2147483648 GC_PRINT_STATS=1 GC_LOG_FILE="$GCLOG" \
              ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local,daemon \
              -m soak --soak-seed="$i" \
              --soak-report="${{ github.workspace }}/soak-nix_2_35-run$i.json" \
              2>&1 | tee "$RUNLOG" || status=$?
            cat "$RUNLOG" >> "$LOGFILE"
            echo "=== TSAN run $i attempt $attempt exit status: $status ===" | tee -a "$LOGFILE"
            # Read the attempt, and not the whole log. The log holds
            # every earlier attempt, so a match there says nothing
            # about this one.
            if grep -q "ThreadSanitizer: data race" "$RUNLOG"; then
              echo "TSAN data race detected on run $i -- stopping early" | tee -a "$LOGFILE"
              race_found=1
              rm -f "$RUNLOG" "$GCLOG"
              break 2
            fi
            if [ "$status" -eq 134 ] && grep -q "Signals delivery fails" "$RUNLOG"; then
              aborts=$((aborts + 1))
              rm -f "$RUNLOG"
              echo "=== Boehm resend log of run $i attempt $attempt ===" | tee -a "$LOGFILE"
              grep -E "Resent|Lost some threads|stop_world" "$GCLOG" | tail -40 | tee -a "$LOGFILE" || true
              rm -f "$GCLOG"
              echo "Boehm could not stop the world on run $i (issue #69). Tolerated aborts: $aborts of $ABORT_BUDGET." | tee -a "$LOGFILE"
              if [ "$aborts" -gt "$ABORT_BUDGET" ]; then
                echo "::error::the collector aborted more than $ABORT_BUDGET times -- see issue #69"
                real_failure=1
                break 2
              fi
              continue
            fi
            rm -f "$RUNLOG" "$GCLOG"
            if [ "$status" -ne 0 ]; then
              echo "::error::run $i failed with status $status -- see log above"
              real_failure=1
              break 2
            fi
            break
          done
        done
        echo "=== TSAN soak summary: $aborts tolerated collector abort(s) of a budget of $ABORT_BUDGET ===" | tee -a "$LOGFILE"
        if [ "$race_found" -eq 1 ]; then
          echo "::error::genuine ThreadSanitizer data race detected -- see log above"
          exit 1
        fi
        if [ "$real_failure" -eq 1 ]; then
          exit 1
        fi
        exit 0
    - name: Run TSAN-instrumented concurrency tests (single pass, local+daemon backends)
      timeout-minutes: 20
      run: |
        set -o pipefail
        LOGFILE="${{ github.workspace }}/tsan-output-broad-nix_2_35.log"
        status=0
        unshare --user --map-root-user --mount --pid --fork --mount-proc env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_RPC_TIMEOUT=30 NANOPYNIX_TEST_SANITIZER=tsan PYTHONDONTWRITEBYTECODE=1 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local,daemon -m concurrency \
          2>&1 | tee -a "$LOGFILE" || status=$?
        echo "=== TSAN broad pass exit status: $status ===" | tee -a "$LOGFILE"
        if grep -q "ThreadSanitizer: data race" "$LOGFILE"; then
          echo "::error::genuine ThreadSanitizer data race detected -- see log above"
          exit 1
        fi
        if grep -qE "[0-9]+ (failed|error)|Fatal Python error|pthread_kill failed at suspend" "$LOGFILE"; then
          echo "::error::pytest reported a real failure, or the process crashed -- see log above"
          exit 1
        fi
        exit 0
    - name: Upload TSAN output (nix_2_35)
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: tsan-race-report-nix_2_35
        path: |
          ${{ github.workspace }}/tsan-output-nix_2_35.log
          ${{ github.workspace }}/tsan-output-broad-nix_2_35.log
          ${{ github.workspace }}/soak-nix_2_35-run*.json
      if: ${{ !cancelled() }}
    timeout-minutes: 165
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-tsan-nix_2_35,')
    runs-on: ubuntu-24.04
  test-daemon-git:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix git
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-git" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix git (full suite, daemon backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends daemon \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (git, daemon backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends daemon \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-daemon-git
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-daemon-git
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: daemon-git
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: daemon-git
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-daemon-git,')
    runs-on: ubuntu-24.04
  test-daemon-nix_2_31:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix nix_2_31
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_31" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix nix_2_31 (full suite, daemon backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends daemon \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (nix_2_31, daemon backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends daemon \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-daemon-nix_2_31
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-daemon-nix_2_31
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: daemon-nix_2_31
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: daemon-nix_2_31
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-daemon-nix_2_31,')
    runs-on: ubuntu-24.04
  test-daemon-nix_2_34:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix nix_2_34
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_34" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix nix_2_34 (full suite, daemon backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends daemon \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (nix_2_34, daemon backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends daemon \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-daemon-nix_2_34
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-daemon-nix_2_34
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: daemon-nix_2_34
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: daemon-nix_2_34
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-daemon-nix_2_34,')
    runs-on: ubuntu-24.04
  test-daemon-nix_2_35:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix nix_2_35
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_35" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix nix_2_35 (full suite, daemon backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends daemon \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (nix_2_35, daemon backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends daemon \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-daemon-nix_2_35
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-daemon-nix_2_35
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: daemon-nix_2_35
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: daemon-nix_2_35
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-daemon-nix_2_35,')
    runs-on: ubuntu-24.04
  test-local-git:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix git
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-git" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix git (full suite, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (git, local backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-local-git
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-local-git
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: local-git
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: local-git
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-local-git,')
    runs-on: ubuntu-24.04
  test-local-nix_2_31:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix nix_2_31
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_31" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix nix_2_31 (full suite, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (nix_2_31, local backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-local-nix_2_31
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-local-nix_2_31
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: local-nix_2_31
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: local-nix_2_31
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-local-nix_2_31,')
    runs-on: ubuntu-24.04
  test-local-nix_2_34:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix nix_2_34
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_34" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix nix_2_34 (full suite, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (nix_2_34, local backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-local-nix_2_34
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-local-nix_2_34
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: local-nix_2_34
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: local-nix_2_34
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-local-nix_2_34,')
    runs-on: ubuntu-24.04
  test-local-nix_2_35:
    steps:
    - timeout-minutes: 10
      uses: actions/checkout@main
    - timeout-minutes: 15
      uses: cachix/install-nix-action@master
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
    - timeout-minutes: 15
      uses: cachix/cachix-action@master
      with:
        name: lillecarl
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        useDaemon: false
    - name: Build nanopynix test runner for Nix nix_2_35
      timeout-minutes: 30
      run: nix build ".#nanopynix-tests-nix_2_35" --out-link result --print-build-logs
        --print-out-paths
    - name: Verify test runner closure after build
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    - name: Enable Nix sandbox namespaces
      timeout-minutes: 5
      run: |
        sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
        sudo sysctl -w kernel.unprivileged_userns_clone=1
        sudo sysctl -w kernel.core_pattern=/tmp/core.%e.%p
        unshare --user --map-root-user --mount --pid --fork --mount-proc true
    - name: Test nanopynix against Nix nix_2_35 (full suite, local backend)
      timeout-minutes: 30
      run: |
        set -o pipefail
        paths_to_delete="${{ github.workspace }}/nanopynix-test-store-paths.txt"
        rm -f "$paths_to_delete"
        # Not stderr: pytest captures fd 2 per test by default and only
        # prints the buffer when that test fails, so a SIGSEGV takes the
        # GC thread log down with it -- a full run that crashed produced
        # zero diagnostic lines. A file is outside pytest's capture.
        gc_thread_log="${{ github.workspace }}/gc-thread-debug.log"
        rm -f "$gc_thread_log"
        status=0
        # NANOPYNIX_COVERAGE rather than pytest-cov's --cov: the runner
        # then measures with `coverage run` and combines after pytest
        # exits. pytest-cov combines *inside* the run, alongside live
        # evaluator threads and forkserver workers, and that combine
        # intermittently failed with "database is locked" -- exit 3 on a
        # job whose tests had all passed. See nanopynix/tests.nix.
        env NANOPYNIX_CORE_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG=1 NANOPYNIX_GC_THREAD_DEBUG_FILE="$gc_thread_log" NANOPYNIX_RPC_TIMEOUT=30 PYTHONDONTWRITEBYTECODE=1 COVERAGE_FILE=${{ github.workspace }}/.coverage NANOPYNIX_COVERAGE=1 NANOPYNIX_COVERAGE_XML=${{ github.workspace }}/coverage.xml NANOPYNIX_TEST_DELETE_PATHS_FILE="$paths_to_delete" \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --run-temp-store-builds --nix-test-backends local \
          -m "not soak" \
          --junitxml=${{ github.workspace }}/junit.xml \
          2>&1 | tee ${{ github.workspace }}/test-gdb-output.log || status=$?
        # Only on a crash: this file has one line per evaluator thread
        # registration and is long, so it is worth the log space solely
        # when there is a faulting LWP to correlate it against.
        if [ "$status" -gt 128 ] && [ -s "$gc_thread_log" ]; then
          {
            echo "=== Boehm GC thread registration log ($(wc -l <"$gc_thread_log") lines) ==="
            cat "$gc_thread_log"
          } >> ${{ github.workspace }}/test-gdb-output.log
        fi
        if [ -s "$paths_to_delete" ]; then
          nix store delete --stdin < "$paths_to_delete" || true
        fi
        exit "$status"
    - name: Run the concurrency soak (nix_2_35, local backend)
      timeout-minutes: 15
      run: |
        set -o pipefail
        env NANOPYNIX_CORE_DEBUG=1 PYTHONDONTWRITEBYTECODE=1 NANOPYNIX_RPC_TIMEOUT=30 \
          ./result/bin/nanopynix-tests --verbose --tb=short -rsxXfE --capture=no --run-temp-store-builds --nix-test-backends local \
          -m soak
    - name: Upload test output
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: test-output-local-nix_2_35
        path: ${{ github.workspace }}/test-gdb-output.log
      if: ${{ !cancelled() }}
    - name: Upload Boehm GC thread registration log
      timeout-minutes: 15
      uses: actions/upload-artifact@main
      with:
        name: gc-thread-debug-local-nix_2_35
        path: ${{ github.workspace }}/gc-thread-debug.log
      if: ${{ !cancelled() }}
    - name: Upload coverage reports to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/coverage.xml
        flags: local-nix_2_35
      if: ${{ !cancelled() }}
    - name: Upload test results to Codecov
      timeout-minutes: 10
      uses: codecov/codecov-action@main
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ${{ github.workspace }}/junit.xml
        flags: local-nix_2_35
        report_type: test_results
      if: ${{ !cancelled() }}
    - name: Verify test runner closure after tests
      timeout-minutes: 15
      run: nix store verify --recursive --no-trust "$(readlink -f result)"
    timeout-minutes: 215
    if: github.event_name != 'workflow_dispatch' || inputs.jobs == '' || contains(format(',{0},',
      inputs.jobs), ',test-local-nix_2_35,')
    runs-on: ubuntu-24.04
