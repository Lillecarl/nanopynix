# TODO: Test Trust & Signing Operations

Implement functional tests for `pynixd`'s signing and signature management logic.

## Operations to Test
*   **`SignPathInfo` (Custom Extension)**:
    *   Verify that `pynixd` can sign a `ValidPathInfo` using its configured secret key.
    *   Verify that signatures are persisted in `pynixd`'s local SQLite DB.
    *   Verify that it correctly calls `AddSignatures` on the backend.
*   **`AddSignatures`**:
    *   Verify that signatures provided by the client are correctly forwarded to the backend store.

## Verification Criteria
- [ ] Signatures generated by `pynixd` are valid and verifiable by `nix store verify`.
- [ ] Signatures are durable across `pynixd` restarts (SQLite check).
- [ ] No duplicate signatures are added if the path is already signed by the same key.
