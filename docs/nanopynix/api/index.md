# API reference

nanopynix has two public async APIs: the worker-backed top-level package and
the in-process `nanopynix.inproc` module. See {doc}`../architecture` for how
they compare.

```{toctree}
:maxdepth: 1

Worker (L3) <worker>
Inproc (L2) <inproc>
settings
models
protocols
exceptions
primops
```
